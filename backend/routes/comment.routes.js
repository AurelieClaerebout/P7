// const express = require("express");
// const router = express.Router();

// const commentCtrl = require("../controllers/comment.controller");
// const authJwt = require("../middleware/authJwt");
// const multer = require("../middleware/multer-config");

// router.post("/:postId", authJwt, multer, commentCtrl.createComment);

// router.get("/", authJwt, commentCtrl.findAllComment);
// router.get("/:postId", authJwt, commentCtrl.findCommentPost);
// router.get("/:postId/:id", authJwt, commentCtrl.findOneComment);
// router.put("/:id", authJwt, multer, commentCtrl.modifyComment);
// router.delete("/:id", authJwt, commentCtrl.deleteComment);

// module.exports = router;
